postgres:
  image: postgres
  env_file: secrets.env

web:
  build: .
  env_file: secrets.env
  ports:
    - '80:8000'
  links:
    - postgres

send-summary-emails:
  build: .
  env_file: secrets.env
  links:
    - postgres
  command: ganttchartsctl send-summary-emails --forever

backup:
  build: tools/backup
  env_file: secrets/backup.env
  links:
    - postgres
